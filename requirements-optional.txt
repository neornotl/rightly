# Optional adapters - install only when needed.
# ASR: PhoWhisper (faster-whisper). Model weights are downloaded at runtime,
# NOT by pip. Verify resources first (docs/hardware_benchmark_plan.md).
faster-whisper>=1.0

# LLM cloud backends
google-genai>=1.0
groq>=0.11

# TTS
edge-tts>=7.0

# UI
streamlit>=1.40
